#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Where the code starts
 * Return: Always 0 (Success)
 */

int main()
{
	int i;

	for (i = '0'; i < '10'; i++)
		printf("%d\n", i);
	return (0);
}
