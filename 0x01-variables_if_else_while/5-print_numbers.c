#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Where the code starts
 * Return: Always 0 (Success)
 */

int main(void)
{
	int m;

	for (m = '0'; m <= '9'; m++)
		printf("%d", m);
	printf("\n");
	return (0);
}
